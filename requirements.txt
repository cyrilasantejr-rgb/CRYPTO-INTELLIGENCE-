# Base dependencies — grows per phase, pin versions as they're introduced.

python-dotenv>=1.0
pydantic>=2.6
requests>=2.31

# Phase 1: market ingestion

# Phase 2: S3 bronze layer
boto3>=1.34
pyarrow>=16.0
moto[s3]>=5.0  # test-only: mocks S3 in unit tests, no real AWS needed

# Testing
pytest>=8.0
pytest-cov>=5.0

# Linting / formatting
ruff>=0.4
black>=24.0
