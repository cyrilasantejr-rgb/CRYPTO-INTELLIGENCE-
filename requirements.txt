# Base dependencies — grows per phase, pin versions as they're introduced.

python-dotenv>=1.0
pydantic>=2.6
requests>=2.31

# Phase 1: market ingestion

# Testing
pytest>=8.0
pytest-cov>=5.0

# Linting / formatting
ruff>=0.4
black>=24.0
